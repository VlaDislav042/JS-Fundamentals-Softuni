function reading(numberPages, pagesPerOne, numberDays) {
    let totalTime = numberPages / pagesPerOne;
    let requiredHours = totalTime / numberDays;
    console.log(requiredHours);
}
reading(432,
    15 ,
    4 
    
    );